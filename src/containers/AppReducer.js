const initialState = {
  isSignedIn: true,
  username: "Jester Adduru",
  type: "overseer",
};

export const loginUser = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};
